const POSITIVE_CONFIRM = ['y', 'Y'];
const CONFIRM_OPTIONS = ['N', 'n'].concat(POSITIVE_CONFIRM);
const PRIMITIVE_TYPES = ['integer', 'string', 'number'];

module.exports = {
  POSITIVE_CONFIRM,
  CONFIRM_OPTIONS,
  PRIMITIVE_TYPES,
};
